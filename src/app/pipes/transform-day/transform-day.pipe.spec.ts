import { TransformDayPipe } from './transform-day.pipe';

describe('TransformDayPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformDayPipe();
    expect(pipe).toBeTruthy();
  });
});
